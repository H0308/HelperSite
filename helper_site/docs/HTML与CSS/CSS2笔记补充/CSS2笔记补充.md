# CSS2笔记补充

## CSS2字体属性

文本间距：根据文档介绍文本间距有`word-spacing`和`letter-spacing`。如果是英文单词，对于`word-spacing`来说，那么修改的就是每个单词之间的间距，而对于`letter-spacing`来说修改的就是每个字母之间的间距。如果是中文单词，`letter-spacing`效果于英文类似，但是对于`word-spacing`来说，默认不会对任何中文字符进行分隔，除非两个中文字中间存在空格

