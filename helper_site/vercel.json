{
    "version": 2,
    "builds": [{
        "src": "requirements.txt",
        "use": "@vercel/python"
    }],

    "buildCommand": "pip install -r requirements.txt && mkdocs build",
    "outputDirectory": "site"
}
